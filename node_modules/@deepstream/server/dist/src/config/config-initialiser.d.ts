import { DeepstreamConfig, DeepstreamServices } from '@deepstream/types';
import Deepstream from '../deepstream.io';
import { InitialLogs } from './js-yaml-loader';
export declare const mergeConnectionOptions: (config: any) => void;
/**
 * Registers plugins by name. Useful when wanting to include
 * custom plugins in a binary
 */
export declare const registerPlugin: (name: string, construct: Function) => void;
/**
 * Takes a configuration object and instantiates functional properties.
 * CLI arguments will be considered.
 */
export declare const initialize: (deepstream: Deepstream, config: DeepstreamConfig, initialLogs?: InitialLogs) => {
    config: DeepstreamConfig;
    services: DeepstreamServices;
};
