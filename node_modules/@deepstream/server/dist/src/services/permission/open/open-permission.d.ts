import { Message } from '../../../constants';
import { DeepstreamPlugin, DeepstreamPermission, PermissionCallback, SocketWrapper } from '@deepstream/types';
/**
 * The open permission handler allows any action to occur without applying
 * any permissions.
 */
export declare class OpenPermission extends DeepstreamPlugin implements DeepstreamPermission {
    description: string;
    /**
    * Allows any action by an user
    */
    canPerformAction(socketWrapper: SocketWrapper, message: Message, callback: PermissionCallback, passItOn: any): void;
}
