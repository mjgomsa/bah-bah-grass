import { DeepstreamTelemetry, DeepstreamPlugin, DeepstreamServices, DeepstreamConfig } from '@deepstream/types';
export interface DeepstreamIOTelemetryOptions {
    enabled: boolean;
    debug: boolean;
    deploymentId: string;
}
export declare class DeepstreamIOTelemetry extends DeepstreamPlugin implements DeepstreamTelemetry {
    private pluginOptions;
    private services;
    private config;
    description: string;
    private logger;
    constructor(pluginOptions: DeepstreamIOTelemetryOptions, services: DeepstreamServices, config: DeepstreamConfig);
    init(): void;
    whenReady(): Promise<void>;
    close(): Promise<void>;
    private sendReport;
}
