import { DeepstreamPlugin, DeepstreamCache, StorageWriteCallback, StorageReadCallback, StorageHeadBulkCallback, StorageHeadCallback } from '@deepstream/types';
export declare class LocalCache extends DeepstreamPlugin implements DeepstreamCache {
    description: string;
    private data;
    head(recordName: string, callback: StorageHeadCallback): void;
    headBulk(recordNames: string[], callback: StorageHeadBulkCallback): void;
    set(key: string, version: number, data: any, callback: StorageWriteCallback): void;
    get(key: string, callback: StorageReadCallback): void;
    delete(key: string, callback: StorageWriteCallback): void;
    deleteBulk(keys: string[], callback: StorageWriteCallback): void;
}
export default LocalCache;
