import { DeepstreamPlugin, DeepstreamMonitoring, SocketData, LOG_LEVEL, EVENT } from '@deepstream/types';
import { Message } from '../../constants';
export declare class NoopMonitoring extends DeepstreamPlugin implements DeepstreamMonitoring {
    description: string;
    onErrorLog(loglevel: LOG_LEVEL, event: EVENT, logMessage: string): void;
    onLogin(allowed: boolean, endpointType: string): void;
    onMessageReceived(message: Message, socketData: SocketData): void;
    onMessageSend(message: Message): void;
    onBroadcast(message: Message, count: number): void;
}
