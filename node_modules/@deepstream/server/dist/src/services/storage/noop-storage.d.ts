import { DeepstreamPlugin, StorageWriteCallback, StorageReadCallback, DeepstreamStorage } from '@deepstream/types';
export declare class NoopStorage extends DeepstreamPlugin implements DeepstreamStorage {
    description: string;
    set(key: string, version: number, data: any, callback: StorageWriteCallback): void;
    get(key: string, callback: StorageReadCallback): void;
    delete(key: string, callback: StorageWriteCallback): void;
    deleteBulk(key: string[], callback: StorageWriteCallback): void;
}
