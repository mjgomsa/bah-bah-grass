import { DeepstreamConfig, DeepstreamServices, DeepstreamPlugin, SubscriptionRegistryFactory, SubscriptionRegistry } from '@deepstream/types';
import { TOPIC } from '../../constants';
export declare class DefaultSubscriptionRegistryFactory extends DeepstreamPlugin implements SubscriptionRegistryFactory {
    private pluginOptions;
    private services;
    private config;
    description: string;
    private subscriptionRegistries;
    constructor(pluginOptions: any, services: Readonly<DeepstreamServices>, config: Readonly<DeepstreamConfig>);
    getSubscriptionRegistry(topic: TOPIC, clusterTopic: TOPIC): SubscriptionRegistry;
    getSubscriptionRegistries(): Map<TOPIC, SubscriptionRegistry>;
}
