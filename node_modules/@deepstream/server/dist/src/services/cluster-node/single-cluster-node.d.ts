import { TOPIC, Message } from '../../constants';
import { DeepstreamClusterNode, DeepstreamPlugin } from '@deepstream/types';
export declare class SingleClusterNode extends DeepstreamPlugin implements DeepstreamClusterNode {
    description: string;
    sendDirect(serverName: string, message: Message, metaData?: any): void;
    send(message: Message, metaData?: any): void;
    subscribe(stateRegistryTopic: TOPIC, callback: Function): void;
    close(): Promise<void>;
}
