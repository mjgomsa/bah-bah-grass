import { WebSocketServerConfig } from '../../base/connection-endpoint';
import BaseWebsocketConnectionEndpoint from '../../base/connection-endpoint';
import { DeepstreamServices, DeepstreamConfig, UnauthenticatedSocketWrapper, WebSocketConnectionEndpoint } from '@deepstream/types';
export declare class WSTextConnectionEndpoint extends BaseWebsocketConnectionEndpoint implements WebSocketConnectionEndpoint {
    wsOptions: WebSocketServerConfig;
    description: string;
    private pingMessage;
    constructor(wsOptions: WebSocketServerConfig, services: DeepstreamServices, config: DeepstreamConfig);
    init(): Promise<void>;
    onConnection(socketWrapper: UnauthenticatedSocketWrapper): void;
    private sendPing;
}
