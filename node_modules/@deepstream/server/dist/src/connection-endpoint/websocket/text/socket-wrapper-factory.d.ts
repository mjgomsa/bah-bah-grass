import { ParseResult, Message } from '../../../constants';
import { SocketConnectionEndpoint, DeepstreamServices } from '@deepstream/types';
import { WebSocketServerConfig } from '../../base/connection-endpoint';
import { WSSocketWrapper } from '../../base/socket-wrapper';
export declare class TextWSSocketWrapper extends WSSocketWrapper<string> {
    socketType: string;
    getMessage(message: Message): string;
    getAckMessage(message: Message): string;
    parseMessage(message: string): ParseResult[];
    parseData(message: Message): true | Error;
}
export declare const createWSSocketWrapper: (socket: any, handshakeData: any, services: DeepstreamServices, config: WebSocketServerConfig, connectionEndpoint: SocketConnectionEndpoint) => TextWSSocketWrapper;
