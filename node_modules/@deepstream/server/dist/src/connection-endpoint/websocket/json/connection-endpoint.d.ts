import WebsocketConnectionEndpoint, { WebSocketServerConfig } from '../../base/connection-endpoint';
import { DeepstreamServices, DeepstreamConfig } from '@deepstream/types';
export declare class WSJSONConnectionEndpoint extends WebsocketConnectionEndpoint {
    wsOptions: WebSocketServerConfig;
    description: string;
    constructor(wsOptions: WebSocketServerConfig, services: DeepstreamServices, config: DeepstreamConfig);
    init(): void;
}
