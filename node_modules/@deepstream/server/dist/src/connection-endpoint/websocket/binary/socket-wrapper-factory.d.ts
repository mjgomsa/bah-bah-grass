import { ParseResult, Message } from '../../../constants';
import { WebSocketServerConfig } from '../../base/connection-endpoint';
import { SocketConnectionEndpoint, DeepstreamServices } from '@deepstream/types';
import { WSSocketWrapper } from '../../base/socket-wrapper';
export declare class WSBinarySocketWrapper extends WSSocketWrapper<Uint8Array> {
    socketType: string;
    getAckMessage(message: Message): Uint8Array;
    getMessage(message: Message): Uint8Array;
    parseMessage(message: ArrayBuffer): ParseResult[];
    parseData(message: Message): true | Error;
}
export declare const createWSSocketWrapper: (socket: any, handshakeData: any, services: DeepstreamServices, config: WebSocketServerConfig, connectionEndpoint: SocketConnectionEndpoint) => WSBinarySocketWrapper;
