/// <reference types="node" />
import { ParseResult } from '../types/messages';
export interface RawMessage {
    fin: boolean;
    topic: number;
    action: number;
    meta?: Buffer;
    payload?: Buffer;
    rawHeader: Buffer;
}
export declare function parse(data: Uint8Array): Array<ParseResult>;
export declare function parseData(message: any): true | Error;
